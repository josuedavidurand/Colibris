import { FiltreDatalistPipe } from './filtreDatalist.pipe';

describe('FiltreDatalistPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreDatalistPipe();
    expect(pipe).toBeTruthy();
  });
});
